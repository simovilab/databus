# Keep the pnpm store on a native Docker volume (see compose.dev.yml: ui_pnpm_store)
# instead of the default project-root location, which lands on the macOS bind
# mount and causes "ERR_PNPM_Unknown system error -35" during cross-device copies.
store-dir=/pnpm-store
