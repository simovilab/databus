"""Django AppConfig for the realtime_engine app."""

from django.apps import AppConfig


class RealtimeEngineConfig(AppConfig):
    """Configure the realtime_engine Django app."""

    name = "realtime_engine"
