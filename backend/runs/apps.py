"""Django AppConfig for the runs app."""

from django.apps import AppConfig


class RunsConfig(AppConfig):
    """Django app configuration for `runs` (run lifecycle, detection, progression, telemetry contracts)."""

    name = 'runs'
