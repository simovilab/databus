"""Django AppConfig for the operations app."""

from django.apps import AppConfig


class OperationsConfig(AppConfig):
    """Django app configuration for `operations` (fleet/operator/vehicle/equipment domain models)."""

    name = "operations"
