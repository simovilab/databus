# Django Settings
DEBUG=False
# No separate static file server (nginx/whitenoise) is deployed in
# compose.prod.yml; without this, DEBUG=False means urls.py never registers
# the /static/ URL patterns and collectstatic's output is never served.
DJANGO_SERVE_STATIC=True
